apiVersion: v1
kind: Service
metadata:
  name: cast-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.castService.service.port }}
      targetPort: {{ .Values.castService.containerPort }}
  selector:
    app: cast-service
