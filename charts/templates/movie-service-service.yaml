apiVersion: v1
kind: Service
metadata:
  name: movie-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.movieService.service.port }}
      targetPort: {{ .Values.movieService.containerPort }}
  selector:
    app: movie-service
